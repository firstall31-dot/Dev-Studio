INSERT INTO public.interview_questions (question, answer, difficulty, area, category, tags, favorite, is_global, created_at) VALUES 
('What is the difference between `==` and `===` in JavaScript?', '`==` performs type coercion before comparing, so `''5'' == 5` is `true`. `===` (strict equality) compares both value AND type without coercion, so `''5'' === 5` is `false`. Always prefer `===` to avoid subtle bugs.', 'junior', 'frontend', 'frontend', '{"javascript","basics"}'::text[], false, true, now()),
('What is the CSS box model?', 'Every element is a rectangular box composed of: Content → Padding → Border → Margin. `box-sizing: border-box` makes width/height include padding and border, which is usually what you want. Default `content-box` excludes them.', 'junior', 'frontend', 'frontend', '{"css","layout"}'::text[], false, true, now()),
('Explain React''s reconciliation algorithm (Virtual DOM diffing).', 'React maintains a virtual DOM tree. On state/prop change it creates a new vDOM and diffs it against the previous one (O(n) heuristic). It assumes elements of different types produce different trees; uses `key` to identify list items across renders. Minimal patches are applied to the real DOM.', 'mid', 'frontend', 'frontend', '{"react","performance"}'::text[], false, true, now()),
('What are React hooks rules and why do they exist?', '1. Only call hooks at the top level — not inside loops, conditions, or nested functions. 2. Only call hooks from React function components or custom hooks. These rules exist because React relies on call order to associate hook state with a component instance across renders.', 'mid', 'frontend', 'frontend', '{"react","hooks"}'::text[], false, true, now()),
('What is event delegation in JavaScript?', 'Instead of attaching a listener to every child element, attach one listener to a parent and use `event.target` to identify which child triggered it. Leverages bubbling. More memory-efficient for large lists (e.g., click handler on `<ul>` instead of every `<li>`).', 'mid', 'frontend', 'frontend', '{"javascript","events","performance"}'::text[], false, true, now()),
('What is the difference between `useMemo` and `useCallback`?', '`useMemo` memoizes the *result* of a function call. `useCallback` memoizes the *function reference itself*. Use `useCallback` when passing a stable callback to a memoized child component; use `useMemo` for expensive computed values. Both take a dependency array.', 'mid', 'frontend', 'frontend', '{"react","performance","hooks"}'::text[], true, true, now()),
('How do you optimize a React app with thousands of list items?', '1. Windowing/virtualization (react-window, TanStack Virtual) — only render visible rows. 2. Memoize list items with `React.memo`. 3. Stable keys. 4. Avoid anonymous functions in JSX that create new references. 5. Move state as low as possible to limit re-render scope. 6. Use transitions (`startTransition`) for non-urgent updates.', 'senior', 'frontend', 'frontend', '{"react","performance","virtualization"}'::text[], false, true, now()),
('Explain the Critical Rendering Path and how to optimize it.', 'Browser: HTML → DOM, CSS → CSSOM → Render Tree → Layout → Paint → Composite. Optimizations: defer non-critical JS, inline critical CSS, use `preload` for fonts/key assets, reduce render-blocking resources, minimize layout thrashing (batch DOM reads/writes), use `will-change` or `transform` for composited animations.', 'senior', 'frontend', 'frontend', '{"performance","browser","css"}'::text[], false, true, now()),
('What is hydration in SSR/SSG frameworks and what are hydration mismatches?', 'Hydration is the process of attaching React''s event listeners and state to server-rendered HTML. A mismatch occurs when the client-rendered output differs from the server HTML (e.g., using `Date.now()` or `window` on server). Mismatches cause React to re-render the entire tree client-side, defeating SSR performance benefits. Solutions: `suppressHydrationWarning`, dynamic imports, or deferring browser-only code to `useEffect`.', 'senior', 'frontend', 'frontend', '{"ssr","react","next.js"}'::text[], true, true, now()),
('What is CSS specificity and how is it calculated?', 'Specificity determines which CSS rule wins conflicts. Calculated as (a, b, c): a = inline styles (1,0,0), b = IDs (0,1,0), c = classes/pseudo-classes/attributes (0,0,1), elements/pseudo-elements (0,0,1 too). Higher specificity wins; same specificity → last in source wins. `!important` overrides all (avoid it).', 'mid', 'frontend', 'frontend', '{"css"}'::text[], false, true, now()),
('What is the difference between REST and GraphQL?', 'REST: multiple fixed endpoints, over/under-fetching is common, versioning needed. GraphQL: single endpoint, client specifies exact data shape, solves over/under-fetching, built-in introspection. REST is simpler for simple APIs; GraphQL shines when many clients need different data shapes.', 'junior', 'backend', 'backend', '{"api","rest","graphql"}'::text[], false, true, now()),
('What is the difference between SQL and NoSQL databases?', 'SQL: relational, structured schema, ACID transactions, joins (Postgres, MySQL). NoSQL: flexible schema, horizontal scaling, eventual consistency (MongoDB document, Redis key-value, Cassandra wide-column, Neo4j graph). Choose SQL for complex relations/transactions; NoSQL for high write throughput, flexible schemas, or specific access patterns.', 'junior', 'backend', 'backend', '{"database","sql","nosql"}'::text[], false, true, now()),
('Explain database indexing. When should you NOT add an index?', 'An index is a data structure (usually B-tree) that speeds up reads by avoiding full table scans. Don''t add indexes on: columns with very low cardinality (boolean), tables with heavy write ratios (indexes slow INSERT/UPDATE/DELETE), rarely queried columns. Each index costs storage and write overhead.', 'mid', 'backend', 'backend', '{"database","performance","sql"}'::text[], true, true, now()),
('What is the N+1 query problem and how do you fix it?', 'N+1 occurs when fetching a list of N records and then issuing 1 extra query per record (e.g., loop fetching user''s posts). Fix: eager loading / JOIN queries, DataLoader (batching in GraphQL), or ORM `include`/`preload`. Use query logging to detect it.', 'mid', 'backend', 'backend', '{"database","performance","orm"}'::text[], false, true, now()),
('What are ACID properties in databases?', 'Atomicity: transaction is all-or-nothing. Consistency: DB moves from one valid state to another. Isolation: concurrent transactions don''t interfere. Durability: committed data survives crashes. Postgres is fully ACID; some NoSQL databases trade isolation/consistency for availability/performance.', 'mid', 'backend', 'backend', '{"database","transactions"}'::text[], false, true, now()),
('How does JWT authentication work?', '1. User logs in → server creates a signed JWT (header.payload.signature) with claims. 2. Client stores it (localStorage or httpOnly cookie). 3. Each request includes the JWT. 4. Server verifies signature using secret/public key — no DB lookup needed. Downside: can''t revoke until expiry unless using a token blacklist or short expiry + refresh tokens.', 'mid', 'backend', 'backend', '{"auth","jwt","security"}'::text[], true, true, now()),
('What is database connection pooling and why does it matter?', 'Opening a DB connection is expensive (TCP handshake, auth, memory). A pool pre-creates N connections and reuses them. Without pooling, every request opens/closes a connection — at scale this exhausts DB connection limits and adds latency. Tools: PgBouncer for Postgres, built-in pool in most ORMs. Configure pool size based on CPU cores × 2 as a starting point.', 'senior', 'backend', 'backend', '{"database","performance","postgres"}'::text[], false, true, now()),
('Explain CAP theorem and its practical implications.', 'In a distributed system you can only guarantee 2 of 3: Consistency (all nodes see same data), Availability (every request gets a response), Partition tolerance (system works despite network splits). Networks always partition → real choice is CP (Postgres, HBase) vs AP (Cassandra, DynamoDB eventual). Most systems allow tunable consistency.', 'senior', 'backend', 'backend', '{"distributed-systems","database"}'::text[], false, true, now()),
('How do you design an API rate limiter?', 'Common algorithms: Token Bucket (smooth bursts), Fixed Window Counter (simple, boundary spikes), Sliding Window Log (accurate, memory heavy), Sliding Window Counter (good tradeoff). Implementation: Redis INCR + EXPIRE for distributed state. Track by IP or user ID. Return `429 Too Many Requests` with `Retry-After` header. Consider separate limits per endpoint/tier.', 'senior', 'backend', 'backend', '{"api","rate-limiting","redis"}'::text[], true, true, now()),
('What is the difference between optimistic and pessimistic locking?', 'Pessimistic: lock the row on read (`SELECT FOR UPDATE`), preventing others from modifying until commit. High contention overhead. Optimistic: no lock on read; on write, check a version/timestamp column — if changed, abort and retry. Best when conflicts are rare. Optimistic is used by most ORMs by default.', 'mid', 'backend', 'backend', '{"database","concurrency"}'::text[], false, true, now()),
('What is the difference between a Docker image and a container?', 'An image is a read-only blueprint (layers of filesystem changes + metadata). A container is a running instance of an image with its own writable layer, network, and process space. Multiple containers can run from the same image simultaneously.', 'junior', 'devops', 'devops', '{"docker","containers"}'::text[], false, true, now()),
('What is CI/CD and why is it important?', 'CI (Continuous Integration): automatically build and test code on every push, catching bugs early. CD (Continuous Delivery/Deployment): automatically deploy validated code to staging or production. Together they reduce release risk, shorten feedback loops, and enable frequent reliable releases.', 'junior', 'devops', 'devops', '{"ci-cd","automation"}'::text[], false, true, now()),
('Explain the difference between horizontal and vertical scaling.', 'Vertical (scale up): add more CPU/RAM to existing server. Simple but has limits and single point of failure. Horizontal (scale out): add more servers behind a load balancer. Requires stateless services (session in DB/Redis) but provides near-unlimited scale and redundancy. Cloud-native apps favor horizontal.', 'mid', 'devops', 'devops', '{"scalability","architecture"}'::text[], false, true, now()),
('What is Kubernetes and what problems does it solve?', 'Kubernetes (K8s) is a container orchestration platform. It solves: scheduling containers across nodes, auto-scaling (HPA), self-healing (restarts failed pods), rolling updates with zero downtime, service discovery, secret/config management, and storage orchestration. Overkill for small teams; powerful for microservices at scale.', 'mid', 'devops', 'devops', '{"kubernetes","containers","orchestration"}'::text[], true, true, now()),
('What is Infrastructure as Code (IaC) and name some tools?', 'IaC means defining and provisioning infrastructure through code files (version-controlled, reviewable, repeatable) instead of manual UI clicks. Tools: Terraform (multi-cloud, declarative), Pulumi (code in TS/Python), AWS CDK, Ansible (configuration management), Helm (K8s package manager). Benefits: reproducibility, drift detection, disaster recovery.', 'mid', 'devops', 'devops', '{"iac","terraform","devops"}'::text[], false, true, now()),
('What is a blue-green deployment?', 'Two identical production environments (blue = current, green = new). Deploy new version to green, run smoke tests, then switch traffic (load balancer or DNS) from blue to green. Instant rollback: just switch back. Cost: double infrastructure during transition. Variant: canary deployment gradually shifts % of traffic.', 'mid', 'devops', 'devops', '{"deployment","zero-downtime"}'::text[], true, true, now()),
('How do you handle secrets in a CI/CD pipeline securely?', 'Never store secrets in code or plain env files. Use: CI secrets store (GitHub Actions secrets, GitLab CI variables), cloud vaults (AWS Secrets Manager, HashiCorp Vault), K8s Secrets (encrypted at rest). Rotate secrets regularly. Audit secret access logs. Use short-lived credentials (IAM roles, OIDC) over long-lived keys. Mask secrets in logs.', 'senior', 'devops', 'devops', '{"security","secrets","ci-cd"}'::text[], false, true, now()),
('Explain the concept of observability and its three pillars.', 'Observability is the ability to understand internal system state from external outputs. Three pillars: 1) Logs — structured event records (use JSON, add correlation IDs). 2) Metrics — numeric measurements over time (latency p95/p99, error rate, throughput — USE method). 3) Traces — request flow across distributed services. Tools: OpenTelemetry, Datadog, Grafana/Prometheus, Jaeger.', 'senior', 'devops', 'devops', '{"observability","monitoring","sre"}'::text[], true, true, now()),
('What is a multi-stage Docker build and why use it?', 'Multi-stage builds use multiple `FROM` statements in one Dockerfile. Build dependencies (compiler, test tools) are in early stages; only artifacts are copied to the final minimal image. Result: production image has no build tools, drastically smaller (e.g., 1GB Node build → 100MB Alpine runtime). Improves security and pull times.', 'senior', 'devops', 'devops', '{"docker","optimization","security"}'::text[], false, true, now()),
('What is the difference between a load balancer and an API gateway?', 'Load balancer: distributes traffic across server instances (L4 TCP or L7 HTTP), health checks, sticky sessions. API Gateway: higher-level — handles auth, rate limiting, request transformation, routing to multiple backend services, SSL termination, caching. Often used together: gateway in front, LB behind each service.', 'mid', 'devops', 'devops', '{"networking","architecture"}'::text[], false, true, now()),
('What is the difference between unit, integration, and E2E tests?', 'Unit: test a single function/class in isolation (mock dependencies). Fast, cheap. Integration: test multiple units working together (DB, external services). Slower, catches interface bugs. E2E: test the full system as a user would (browser, real server). Slow, expensive, most confidence. Follow the Testing Pyramid: many unit, some integration, few E2E.', 'junior', 'testing', 'testing', '{"testing","strategy"}'::text[], true, true, now()),
('What is a test double (mock, stub, spy, fake)?', 'Test doubles replace real dependencies in tests. Stub: returns hardcoded data. Mock: stub + verifies it was called correctly. Spy: wraps real implementation + records calls. Fake: working lightweight implementation (in-memory DB). Use the least powerful double that makes the test meaningful.', 'junior', 'testing', 'testing', '{"testing","mocking"}'::text[], false, true, now()),
('What is Test-Driven Development (TDD) and what are its benefits?', 'TDD: Red → Green → Refactor. Write a failing test first, make it pass minimally, then refactor. Benefits: forces design thinking, creates living documentation, prevents over-engineering (YAGNI), catches regressions immediately. Criticism: slow for exploratory work, can lead to over-testing implementation details.', 'mid', 'testing', 'testing', '{"tdd","methodology"}'::text[], false, true, now()),
('What is code coverage and what are its limitations?', 'Coverage measures % of code lines/branches executed during tests. 100% coverage does NOT mean bug-free — tests may pass without meaningful assertions. It misses: wrong business logic, concurrency bugs, integration failures. Use coverage as a minimum bar (e.g., 80%) to find untested areas, not as a quality metric.', 'mid', 'testing', 'testing', '{"testing","coverage","metrics"}'::text[], false, true, now()),
('How do you test asynchronous code in JavaScript?', '1. Return a Promise from the test. 2. Use `async/await`. 3. Use `done` callback (older Jest). 4. Use `vi.useFakeTimers()` / `jest.useFakeTimers()` to control time-based async. 5. For HTTP: use `msw` (Mock Service Worker) to intercept network requests. Always handle rejections — unhandled rejections can silently pass tests.', 'mid', 'testing', 'testing', '{"javascript","async","testing"}'::text[], false, true, now()),
('What is the difference between Vitest and Jest?', 'Both are JavaScript test runners with similar APIs. Vitest: Vite-native, uses Vite''s transform pipeline (fast, ESM-first, no config for modern stacks), watch mode is instant. Jest: mature, wider ecosystem, transpiles via Babel/tsc. For Vite/React projects Vitest is preferred; Jest for legacy CRA/Node projects.', 'mid', 'testing', 'testing', '{"vitest","jest","tooling"}'::text[], false, true, now()),
('How do you approach testing a React component that fetches data?', '1. Use `@testing-library/react` (query by accessible role, not implementation). 2. Mock network with `msw` (Mock Service Worker) — intercepts at the network layer, not the fetch function. 3. Use `screen.findBy*` (async) to wait for data to render. 4. Test loading state, success state, and error state. Avoid testing internal state; test what the user sees.', 'senior', 'testing', 'testing', '{"react","testing-library","msw"}'::text[], true, true, now()),
('What is snapshot testing and when should you use or avoid it?', 'Snapshot tests serialize component output and compare against a stored file. Good for: detecting unintended UI changes in stable components, serializing complex data structures. Avoid when: snapshots are large/unreadable, updated blindly with `--updateSnapshot`, testing implementation details. Prefer explicit assertions for meaningful logic.', 'senior', 'testing', 'testing', '{"testing","snapshots","react"}'::text[], false, true, now()),
('How do you write good E2E tests with Playwright/Cypress?', '1. Use `data-testid` or ARIA roles — not CSS selectors. 2. Test critical user journeys, not every edge case. 3. Use fixtures/factories for test data; clean up after each test. 4. Run against a staging env with seeded data. 5. Parallelize across shards. 6. Retry flaky selectors with auto-wait (Playwright does this). 7. Screenshot on failure for debugging.', 'senior', 'testing', 'testing', '{"e2e","playwright","cypress"}'::text[], true, true, now()),
('What is property-based testing?', 'Instead of example-based tests (specific inputs/outputs), you define properties that should hold for all inputs, and the framework generates hundreds of random inputs to find counterexamples. Tools: fast-check (JS), Hypothesis (Python), QuickCheck (Haskell). Great for: parsers, serializers, algorithms where invariants can be expressed. Hard to write for UIs.', 'mid', 'testing', 'testing', '{"testing","property-based"}'::text[], false, true, now()),
('What is the SOLID principles? Give a brief description of each.', 'S: Single Responsibility — one class, one reason to change. O: Open/Closed — open for extension, closed for modification. L: Liskov Substitution — subclasses must be substitutable for base classes. I: Interface Segregation — many specific interfaces > one general. D: Dependency Inversion — depend on abstractions, not concretions.', 'mid', 'general', 'general', '{"architecture","design-patterns"}'::text[], true, true, now()),
('What is the difference between concurrency and parallelism?', 'Concurrency: dealing with multiple things at once (structure) — e.g., Node.js event loop handles many requests on one thread by interleaving. Parallelism: doing multiple things simultaneously (execution) — requires multiple CPU cores. You can have concurrency without parallelism (single core multitasking).', 'mid', 'general', 'general', '{"concurrency","systems"}'::text[], false, true, now()),
('How do you approach a system design interview?', '1. Clarify requirements (functional + non-functional: QPS, latency, scale). 2. Estimate scale (DAU, storage, bandwidth). 3. High-level design (API, data model, main components). 4. Deep dive on bottlenecks (DB choice, caching, sharding, CDN). 5. Discuss tradeoffs explicitly — no perfect answer. 6. Address reliability (replication, retries, circuit breakers).', 'senior', 'general', 'general', '{"system-design","interview"}'::text[], true, true, now()),
('What is the difference between a monolith, microservices, and modular monolith?', 'Monolith: single deployable unit, simple to develop initially, hard to scale selectively. Microservices: independently deployable services, each owns its data; network overhead, distributed complexity. Modular monolith: clear internal module boundaries, single deployment — best of both worlds for most teams. Start modular monolith, extract services only when you have clear scaling needs.', 'mid', 'general', 'general', '{"architecture","microservices"}'::text[], false, true, now()),
('What is Git rebasing vs merging?', 'Merge: creates a merge commit preserving full branch history. Rebase: replays commits on top of target branch, creating a linear history. Rebase rewrites SHA hashes — never rebase shared/public branches. Use merge for feature → main; rebase for keeping feature branch up-to-date with main locally.', 'junior', 'general', 'general', '{"git","version-control"}'::text[], false, true, now()),
('What is Angular''s component lifecycle and name the key hooks?', 'Angular components go through: ngOnChanges (input changes), ngOnInit (after first change detection), ngDoCheck, ngAfterContentInit, ngAfterContentChecked, ngAfterViewInit, ngAfterViewChecked, ngOnDestroy. Most commonly used: ngOnInit (setup), ngOnDestroy (cleanup subscriptions), ngOnChanges (react to @Input changes).', 'junior', 'frontend', 'Angular', '{"angular","lifecycle","components"}'::text[], false, true, now()),
('How does Angular''s dependency injection system work?', 'Angular has a hierarchical DI system. Services are registered in providers (root injector, module injector, or component injector). When a component requests a service via constructor injection, Angular walks up the injector tree to find a provider. `providedIn: ''root''` makes a service a singleton app-wide. Component-level providers create a new instance per component subtree.', 'mid', 'frontend', 'Angular', '{"angular","dependency-injection","services"}'::text[], false, true, now()),
('What is the difference between Default and OnPush change detection in Angular?', 'Default: Angular checks the entire component tree on every event/async operation (slow for large apps). OnPush: Angular only checks when 1) an @Input reference changes (immutable data), 2) an event originates from the component or its children, 3) async pipe emits, or 4) markForCheck() is called manually. OnPush requires immutable data patterns and delivers significant performance gains.', 'senior', 'frontend', 'Angular', '{"angular","change-detection","performance"}'::text[], true, true, now()),
('What is the difference between structural and attribute directives in Angular?', 'Structural directives change DOM layout by adding/removing elements — prefixed with * (*ngIf, *ngFor, *ngSwitch). They use the `<ng-template>` syntax under the hood. Attribute directives change the appearance or behavior of an existing element without changing structure (ngClass, ngStyle, custom directives using @HostListener/@HostBinding).', 'junior', 'frontend', 'Angular', '{"angular","directives"}'::text[], false, true, now()),
('What is RxJS and how is it central to Angular?', 'RxJS is a library for reactive programming using Observables. Angular uses it throughout: HttpClient returns Observables, Router events are streams, Forms have valueChanges/statusChanges Observables, EventEmitter extends Subject. Key operators: map, filter, switchMap (cancel previous), mergeMap (concurrent), takeUntil (cleanup), combineLatest, debounceTime. Always unsubscribe in ngOnDestroy or use async pipe.', 'mid', 'frontend', 'Angular', '{"angular","rxjs","observables"}'::text[], true, true, now()),
('What are Angular Signals and how do they differ from RxJS?', 'Signals (Angular 16+) are reactive primitives — a signal holds a value and notifies consumers when it changes. Simpler than RxJS for synchronous state: `const count = signal(0); count.set(1); count.update(v => v+1); computed(() => count() * 2)`. Unlike RxJS, signals are synchronous, don''t need subscription management, and integrate with OnPush change detection automatically. RxJS remains better for async event streams.', 'mid', 'frontend', 'Angular', '{"angular","signals","reactivity"}'::text[], false, true, now()),
('How does lazy loading work in Angular?', 'Lazy loading defers loading feature modules/components until their route is activated. In the router: `loadChildren: () => import(''./feature/feature.module'').then(m => m.FeatureModule)` or with standalone components: `loadComponent: () => import(''./feature.component'')`. Reduces initial bundle size. Works with preloading strategies (PreloadAllModules, custom) to fetch lazy chunks in the background after initial load.', 'mid', 'frontend', 'Angular', '{"angular","lazy-loading","performance","routing"}'::text[], false, true, now()),
('What is the difference between Template-driven and Reactive forms in Angular?', 'Template-driven: logic lives in the template (ngModel, ngForm), simple and familiar, less testable, async validation is complex. Reactive: form structure defined in component class (FormControl, FormGroup, FormArray), synchronous access to form tree, fully testable, better for complex dynamic forms. Both use `FormsModule` or `ReactiveFormsModule`. Reactive forms are preferred for non-trivial forms.', 'mid', 'frontend', 'Angular', '{"angular","forms"}'::text[], false, true, now()),
('What is the difference between Vue''s Composition API and Options API?', 'Options API: organizes code by options (data, methods, computed, watch) — familiar but logic for one feature is scattered. Composition API (Vue 3): organizes code by logical concern using `setup()` and composables (reusable functions). Better TypeScript support, easier to extract/reuse logic. Both work in Vue 3; Composition API is recommended for new projects.', 'mid', 'frontend', 'Vue.js', '{"vue","composition-api","options-api"}'::text[], true, true, now()),
('How does Vue 3''s reactivity system work?', 'Vue 3 uses ES Proxy for reactivity. `reactive()` wraps an object in a Proxy that intercepts get/set to track dependencies and trigger updates. `ref()` wraps primitives in an object with a `.value` getter/setter. `computed()` creates lazy, cached derived values. `watch()` and `watchEffect()` run side effects when dependencies change. The tracking is automatic — accessing a reactive value inside a computed/watchEffect registers it as a dependency.', 'mid', 'frontend', 'Vue.js', '{"vue","reactivity","proxy"}'::text[], false, true, now()),
('What is Pinia and why is it preferred over Vuex?', 'Pinia is the official Vue state management library (successor to Vuex). Benefits over Vuex: no mutations (just actions and state), full TypeScript support out of the box, simpler API (no namespaced modules), devtools support, composables-based design. Store: `defineStore(''id'', { state: () => ({}), getters: {}, actions: {} })`. Lightweight and tree-shakeable.', 'junior', 'frontend', 'Vue.js', '{"vue","pinia","state-management"}'::text[], false, true, now()),
('What are Vue''s key directives and what do they do?', 'v-if/v-else/v-else-if: conditionally renders (removes from DOM). v-show: toggles CSS display. v-for: list rendering (always add :key). v-bind (:): dynamically binds attributes/props. v-on (@): attaches event listeners. v-model: two-way data binding. v-slot: scoped slots. Rule: use v-show for frequent toggles (no DOM recreate); v-if for infrequent/conditional rendering.', 'junior', 'frontend', 'Vue.js', '{"vue","directives"}'::text[], false, true, now()),
('What is the difference between computed properties and watchers in Vue?', 'Computed: declarative derived values, cached until dependencies change, returns a value, use for derived UI data. Watch: imperative side effects when a value changes, no return value, use when you need to perform async operations or call APIs in response to state change. watchEffect: runs immediately and re-runs when any reactive dependency changes. Prefer computed over watch when possible.', 'mid', 'frontend', 'Vue.js', '{"vue","computed","watch"}'::text[], false, true, now()),
('How does Svelte fundamentally differ from React and Vue?', 'Svelte is a compiler, not a runtime framework. It compiles components to highly optimized vanilla JavaScript at build time — no virtual DOM, no runtime diffing. Updates are surgical direct DOM operations. Results: smaller bundle size, faster runtime performance, no overhead. Tradeoff: smaller ecosystem, no runtime introspection, compilation step required.', 'mid', 'frontend', 'Svelte', '{"svelte","compiler","performance"}'::text[], true, true, now()),
('What are Svelte stores and how do they enable state sharing?', 'Svelte stores are observable objects with a subscribe method. Built-ins: writable(value), readable(value, start), derived(stores, fn). Any store can be used in any component — not just Svelte. The `$` prefix auto-subscribes and unsubscribes: `$count` in a template subscribes automatically. Custom stores wrap writable with additional logic. No provider/context needed for global state.', 'mid', 'frontend', 'Svelte', '{"svelte","stores","state"}'::text[], false, true, now()),
('What are Svelte 5 Runes?', 'Runes (Svelte 5) replace the compiler magic with explicit reactive primitives: `$state()` for reactive state, `$derived()` for computed values, `$effect()` for side effects, `$props()` for component props. More explicit than Svelte 4''s implicit reactivity (assigning to a variable = reactive). Better TypeScript support and clearer reactivity semantics. Similar intent to Vue''s ref/computed/watchEffect but compiler-powered.', 'mid', 'frontend', 'Svelte', '{"svelte","runes","svelte5"}'::text[], false, true, now()),
('What is the difference between App Router and Pages Router in Next.js?', 'Pages Router (Next.js < 13): file-based routing in /pages, getServerSideProps/getStaticProps for data fetching, all components are client-side by default. App Router (Next.js 13+): file-based routing in /app, React Server Components by default, co-located layouts/loading/error files, Server Actions for mutations, streaming with Suspense. App Router is the recommended path for new projects.', 'mid', 'frontend', 'Next.js', '{"nextjs","app-router","pages-router"}'::text[], true, true, now()),
('What are React Server Components (RSC) and how do they differ from Client Components?', 'Server Components render exclusively on the server — they can fetch data directly (DB, APIs), import server-only modules, and never ship their code to the client. Client Components (`''use client''`) run in the browser and can use hooks, event handlers, browser APIs. RSCs can render Client Components but not vice versa. RSCs reduce JS bundle size and enable direct data access without API layers.', 'mid', 'frontend', 'Next.js', '{"nextjs","rsc","server-components"}'::text[], false, true, now()),
('What is Incremental Static Regeneration (ISR) in Next.js?', 'ISR lets you create or update static pages after build time without a full rebuild. Configure with `revalidate` in App Router (`export const revalidate = 60`). On-demand revalidation via `revalidatePath()` or `revalidateTag()`. Pages are served from cache and regenerated in the background when stale. Best for: e-commerce product pages, blog posts, dashboards that tolerate slight staleness.', 'mid', 'frontend', 'Next.js', '{"nextjs","isr","ssg","caching"}'::text[], false, true, now()),
('What are Server Actions in Next.js?', 'Server Actions (Next.js 14+) are async functions that run on the server, callable from Client Components. Decorated with `''use server''`. Used for form submissions, mutations, database writes — eliminating the need for API routes for simple mutations. They integrate with React''s form action pattern, support progressive enhancement, and can be used with `useFormState` / `useFormStatus` hooks.', 'mid', 'frontend', 'Next.js', '{"nextjs","server-actions","mutations"}'::text[], false, true, now()),
('How does the ASP.NET Core middleware pipeline work?', 'Middleware are components assembled in a pipeline. Each has access to HttpContext and can call the next middleware. Registered via `app.Use()`, `app.Run()` (terminal), `app.Map()` (branch). Order matters: request flows in registration order, response flows in reverse. Common built-ins: UseRouting, UseAuthentication, UseAuthorization, UseCors, UseStaticFiles. Use `app.UseExceptionHandler()` early to catch downstream errors.', 'mid', 'backend', 'ASP.NET Core', '{"aspnet","middleware","pipeline"}'::text[], true, true, now()),
('How does dependency injection work in ASP.NET Core?', 'ASP.NET Core has built-in DI via IServiceCollection. Register services with lifetimes: Transient (new instance per request), Scoped (one per HTTP request), Singleton (one for app lifetime). Inject via constructor parameters — the framework resolves the dependency graph. Use `services.AddScoped<IMyService, MyService>()`. Scoped services are the most common for request-based work like DB contexts.', 'junior', 'backend', 'ASP.NET Core', '{"aspnet","dependency-injection","ioc"}'::text[], false, true, now()),
('What are Minimal APIs in ASP.NET Core and when should you use them?', 'Minimal APIs (ASP.NET Core 6+) define endpoints with less ceremony than controllers: `app.MapGet(''/items'', (MyDb db) => db.Items.ToListAsync())`. No controller classes, attributes-light, co-located handler logic. Best for: microservices, simple CRUD endpoints, performance-critical APIs (lower overhead than controllers). Use controllers when you need complex filter pipelines, model binders, or large teams prefer MVC convention.', 'mid', 'backend', 'ASP.NET Core', '{"aspnet","minimal-apis","endpoints"}'::text[], false, true, now()),
('What is Entity Framework Core and what are its main approaches?', 'EF Core is the ORM for .NET. Two approaches: Code-First (define C# model classes, generate migrations with `dotnet ef migrations add`) and Database-First (scaffold models from existing DB). Key concepts: DbContext manages DB connections and change tracking, DbSet<T> represents a table, LINQ queries translate to SQL, migrations version the schema. Use AsNoTracking() for read-only queries for performance.', 'mid', 'backend', 'ASP.NET Core', '{"aspnet","efcore","orm","database"}'::text[], false, true, now()),
('How does authentication and authorization work in ASP.NET Core?', 'Authentication: who are you? — `AddAuthentication()` + schemes (JWT Bearer, Cookie, OAuth). Use `[Authorize]` attribute or `RequireAuthorization()` for minimal APIs. Authorization: what can you do? — `AddAuthorization()` with policies. Claims-based: check user.HasClaim(). Policy-based: `RequireRole`, `RequireClaim`, custom `IAuthorizationRequirement`. JWT: validate with `AddJwtBearer()`, check signature, issuer, audience, expiry.', 'mid', 'backend', 'ASP.NET Core', '{"aspnet","auth","jwt","security"}'::text[], true, true, now()),
('What is the CQRS pattern and how is it implemented in .NET?', 'CQRS (Command Query Responsibility Segregation) separates read (Query) and write (Command) models. Commands mutate state and return nothing (or a minimal result). Queries read state and return data. Benefits: optimize reads/writes independently, different DB models for reads (denormalized) vs writes (normalized). In .NET: often implemented with MediatR library — handlers implement IRequestHandler<TCommand, TResult>. Pairs well with event sourcing.', 'senior', 'backend', 'ASP.NET Core', '{"aspnet","cqrs","architecture","mediatr"}'::text[], false, true, now()),
('What are Background Services in ASP.NET Core?', 'Background services run long-running tasks alongside the web host. Implement `IHostedService` or extend `BackgroundService` (simpler — override `ExecuteAsync(CancellationToken)`). Registered with `services.AddHostedService<MyWorker>()`. Use cases: polling external APIs, processing message queues, scheduled jobs (use Quartz.NET or HangFire for scheduling). Always respect the CancellationToken for graceful shutdown.', 'mid', 'backend', 'ASP.NET Core', '{"aspnet","background-services","workers"}'::text[], false, true, now()),
('What is the difference between IActionResult and ActionResult<T> in ASP.NET Core?', 'IActionResult: returns any HTTP result (Ok(), NotFound(), BadRequest()). ActionResult<T>: adds generic type so return type is explicit — enables Swagger/OpenAPI to auto-document the success response schema and allows returning T directly (auto-wrapped in 200 OK) or IActionResult for error codes. Always prefer `ActionResult<T>` in controllers for better documentation and type safety.', 'mid', 'backend', 'ASP.NET Core', '{"aspnet","controllers","api"}'::text[], false, true, now()),
('What are the different types of SQL JOINs?', 'INNER JOIN: rows where condition matches in both tables. LEFT JOIN: all rows from left + matching rows from right (NULLs for non-matches). RIGHT JOIN: all rows from right + matching rows from left. FULL OUTER JOIN: all rows from both, NULLs where no match. CROSS JOIN: cartesian product (every combination). SELF JOIN: table joined with itself (hierarchies). LEFT JOIN is the most commonly used after INNER JOIN.', 'junior', 'database', 'SQL Fundamentals', '{"sql","joins","fundamentals"}'::text[], false, true, now()),
('What is database normalization? Explain 1NF, 2NF, 3NF.', 'Normalization reduces redundancy and improves integrity. 1NF: atomic columns (no arrays/repeating groups), each row unique. 2NF: 1NF + no partial dependencies (non-key columns depend on the full primary key, not just part of it — matters for composite keys). 3NF: 2NF + no transitive dependencies (non-key columns depend only on the primary key, not on other non-key columns). BCNF is a stricter form of 3NF.', 'mid', 'database', 'Schema Design', '{"sql","normalization","schema-design"}'::text[], true, true, now()),
('What are the different transaction isolation levels and what problems do they prevent?', 'Problems: Dirty Read (read uncommitted data), Non-repeatable Read (same query gives different results), Phantom Read (new rows appear in range query). Levels (weakest → strongest): READ UNCOMMITTED (no protection), READ COMMITTED (prevents dirty reads — Postgres default), REPEATABLE READ (prevents dirty + non-repeatable — MySQL default for InnoDB), SERIALIZABLE (prevents all — slowest). Higher isolation = less concurrency.', 'mid', 'database', 'Transactions & Concurrency', '{"sql","transactions","isolation","concurrency"}'::text[], true, true, now()),
('What is the difference between WHERE and HAVING in SQL?', 'WHERE filters rows before grouping — it operates on individual rows and cannot use aggregate functions. HAVING filters groups after GROUP BY — it can use aggregate functions (HAVING COUNT(*) > 5). Rule: if you need to filter on an aggregate result, use HAVING; for all other row-level filters, use WHERE (it''s faster as it reduces rows before aggregation).', 'junior', 'database', 'SQL Fundamentals', '{"sql","aggregation","fundamentals"}'::text[], false, true, now()),
('What is a Common Table Expression (CTE) and when should you use it?', 'A CTE (WITH clause) defines a named temporary result set scoped to the query. Benefits: readability (name complex subqueries), reusability within the same query, enables recursive queries (recursive CTE for hierarchies/trees). `WITH cte AS (SELECT ...) SELECT * FROM cte`. CTEs are not materialized by default in most DBs — they''re inlined like views. Use when subqueries become deeply nested and hard to read.', 'mid', 'database', 'SQL Fundamentals', '{"sql","cte","advanced"}'::text[], false, true, now()),
('What is a covering index and how does it eliminate table lookups?', 'A covering index includes all columns needed to satisfy a query — the DB can answer the query entirely from the index without touching the main table (heap/cluster). Example: query `SELECT email FROM users WHERE status = ''active''` is covered by index `(status, email)`. This eliminates the I/O-heavy random access to table rows. In Postgres, use `INCLUDE` clause to add non-key columns: `CREATE INDEX ON users(status) INCLUDE (email)`.', 'senior', 'database', 'Indexing & Performance', '{"database","indexing","performance"}'::text[], true, true, now()),
('What is a composite index and what is the prefix rule?', 'A composite index covers multiple columns: `CREATE INDEX ON orders(user_id, status, created_at)`. The prefix rule: the index is usable for queries that filter on a left-to-right prefix of the index columns. This index supports queries on (user_id), (user_id, status), (user_id, status, created_at) — but NOT on (status) alone or (status, created_at). Column order matters: put equality conditions before range conditions.', 'mid', 'database', 'Indexing & Performance', '{"database","indexing","composite-index"}'::text[], false, true, now()),
('What is Redis and what are its main use cases?', 'Redis is an in-memory data structure store (strings, hashes, lists, sets, sorted sets, streams). Use cases: Caching (TTL-based, LRU eviction), Session storage, Rate limiting (INCR + EXPIRE), Pub/Sub messaging, Leaderboards (sorted sets), Distributed locks (SET NX EX), Job queues (Lists, Streams). Data is optionally persisted (RDB snapshots or AOF log). Single-threaded command execution ensures atomicity.', 'mid', 'database', 'NoSQL & Redis', '{"redis","caching","nosql"}'::text[], true, true, now()),
('When would you choose MongoDB over PostgreSQL?', 'MongoDB: flexible/evolving schema (content management, catalogs), document-centric data with nested structures, high write throughput, horizontal scaling from day one, when you don''t need complex joins. PostgreSQL: relational data with complex queries and joins, ACID transactions, strong consistency requirements, analytics/reporting, JSON support (JSONB) for mixed workloads. For most SaaS apps, PostgreSQL with JSONB covers both needs without operational complexity of MongoDB.', 'mid', 'database', 'NoSQL & Redis', '{"mongodb","postgresql","nosql","sql"}'::text[], false, true, now()),
('What is database sharding and what are its challenges?', 'Sharding partitions data horizontally across multiple database servers (shards) by a shard key (e.g., user_id % N). Enables horizontal write scaling beyond a single node''s capacity. Challenges: cross-shard queries are expensive, joins across shards require application-level merging, resharding when adding nodes is complex, hot shards if key is poorly chosen (uneven distribution), no cross-shard ACID transactions. Alternatives: read replicas, vertical scaling, partitioning within one DB.', 'senior', 'database', 'Scaling & Architecture', '{"database","sharding","scalability"}'::text[], true, true, now()),
('What is a database migration and why is it important?', 'A migration is a versioned, incremental change to the database schema (create table, add column, create index). Migrations are code-reviewed, committed to version control, and applied in order across all environments. Benefits: reproducible schema across dev/staging/production, rollback capability, team collaboration without schema conflicts. Tools: Flyway, Liquibase (language-agnostic), Alembic (Python), EF Core migrations, Drizzle Kit.', 'junior', 'database', 'Schema Design', '{"database","migrations","devops"}'::text[], false, true, now()),
('What is an ORM and what are its tradeoffs?', 'ORM (Object-Relational Mapper) maps database tables to classes/objects, enabling type-safe queries without raw SQL. Benefits: productivity, portability across DBs, migrations tooling, avoids SQL injection by default. Tradeoffs: N+1 query problem if eager loading not configured, generated SQL can be inefficient (use EXPLAIN to verify), abstraction leaks in complex queries, heavy reflection overhead. Solution: use ORM for standard CRUD, raw SQL / query builders for complex queries.', 'mid', 'database', 'ORM & Tools', '{"orm","database","architecture"}'::text[], false, true, now()),
('What is database denormalization and when is it justified?', 'Denormalization intentionally introduces redundancy (duplicating data, pre-computed aggregates) to speed up reads at the cost of write complexity and storage. Justified when: read performance is critical and joins are too slow, the data is mostly read-only, you need to support analytics queries on OLTP data, or building read models for CQRS. Examples: storing user name on orders table, pre-computed order totals. Always measure before denormalizing — premature optimization.', 'senior', 'database', 'Schema Design', '{"database","denormalization","performance"}'::text[], false, true, now()),
('What is Event Sourcing and how does it relate to databases?', 'Event Sourcing stores state as an immutable sequence of events rather than the current state. Instead of UPDATE users SET balance=100, you store BalanceDebited{amount:50}. Current state is derived by replaying events. Benefits: complete audit log, ability to rebuild state at any point, decoupled event consumers. Tradeoffs: complex queries (need projections/read models), eventual consistency, snapshot management for long event histories. Usually paired with CQRS.', 'senior', 'database', 'Scaling & Architecture', '{"database","event-sourcing","architecture"}'::text[], false, true, now()),
('What is the difference between optimistic and pessimistic concurrency control in databases?', 'Pessimistic: acquire a lock before reading/writing (SELECT FOR UPDATE). Blocks concurrent access — safe but reduces throughput. Use for high-contention scenarios like inventory deduction. Optimistic: read without lock, write with a version check (WHERE version = :expected). If version changed, abort and retry. Better throughput when conflicts are rare — common in web apps. Both prevent lost updates. Optimistic is the ORM default (version column).', 'mid', 'database', 'Transactions & Concurrency', '{"database","concurrency","transactions"}'::text[], false, true, now()),
('How does PostgreSQL''s EXPLAIN ANALYZE work and what should you look for?', 'EXPLAIN ANALYZE executes the query and shows the actual execution plan with timing. Key things to look for: Sequential Scan on large tables (add index?), Nested Loop on large sets (may need HashJoin), rows estimate vs actual rows (bad statistics → run ANALYZE), high cost nodes at the top of the plan. Use `EXPLAIN (ANALYZE, BUFFERS)` to see cache hits/misses. Look for hash joins on big result sets, and index scans for filtered queries.', 'mid', 'database', 'Indexing & Performance', '{"postgresql","query-optimization","performance"}'::text[], true, true, now()),
('What is the difference between a primary key and a unique index?', 'Primary key: uniquely identifies each row, cannot be NULL, only one per table, automatically creates a unique index, used as the default join target. Unique index: enforces uniqueness on one or more columns, can allow NULLs (multiple NULLs are allowed in most DBs since NULL ≠ NULL), multiple unique indexes per table, used for business-level uniqueness constraints (email, username). Foreign keys reference primary keys by default.', 'junior', 'database', 'SQL Fundamentals', '{"sql","indexing","fundamentals"}'::text[], false, true, now()),
('What is connection pooling in databases and how do you size the pool?', 'A connection pool maintains a set of pre-established connections reused across requests. Opening a DB connection is expensive (TCP handshake, auth, SSL). Without pooling, each request opens/closes a connection — at scale you hit DB connection limits. Sizing rule of thumb: pool_size = num_cores × 2 + effective_spindle_count (HDD) or just CPU cores × 2 for SSD. Use PgBouncer for Postgres in transaction mode. Monitor: pool exhaustion (requests queuing) = increase pool or reduce query time.', 'mid', 'database', 'ORM & Tools', '{"database","connection-pooling","performance"}'::text[], false, true, now()),
('How do you write integration tests for an ASP.NET Core API?', 'Use `WebApplicationFactory<TProgram>` from Microsoft.AspNetCore.Mvc.Testing. It spins up the full app in-memory with a test HTTP client. Replace real services with fakes: `factory.WithWebHostBuilder(builder => builder.ConfigureTestServices(...))`. Use an in-memory DB or test DB that resets between tests. Assert on response status codes and deserialized body. Add xUnit''s `IClassFixture<WebApplicationFactory<Program>>` for shared factory.', 'mid', 'testing', 'Backend Testing (.NET)', '{"aspnet","testing","integration-tests","xunit"}'::text[], true, true, now()),
('What is xUnit and how does it compare to NUnit in .NET?', 'Both are popular .NET test frameworks. xUnit: constructor injection for setup (no [SetUp]), IDisposable for cleanup, Theory/InlineData for parameterized tests, shared context via IClassFixture. NUnit: [SetUp]/[TearDown] attributes, [TestCase] for parameterized, more attribute-heavy. MSTest: Microsoft''s built-in, less community features. xUnit is the most modern and is used by ASP.NET Core itself — prefer it for new projects. Fluent Assertions is a popular assertion library for all three.', 'junior', 'testing', 'Backend Testing (.NET)', '{"aspnet","xunit","nunit","testing"}'::text[], false, true, now()),
('How do you mock dependencies in .NET unit tests?', 'Use Moq or NSubstitute for mocking interfaces. Moq: `var mock = new Mock<IMyService>(); mock.Setup(m => m.GetById(1)).ReturnsAsync(result); var sut = new MyController(mock.Object);`. Verify calls: `mock.Verify(m => m.Save(It.IsAny<Item>()), Times.Once)`. NSubstitute: cleaner syntax `var sub = Substitute.For<IMyService>()`. Never mock concrete classes — depend on interfaces (ISP principle). Mock at service boundaries, not deep implementation details.', 'mid', 'testing', 'Backend Testing (.NET)', '{"aspnet","moq","mocking","unit-tests"}'::text[], false, true, now()),
('How do you test a Node.js/Express REST API?', 'Supertest: sends HTTP requests to the Express app without a real server. `const res = await request(app).post(''/api/items'').send({name:''test''}).expect(201)`. Use Jest or Vitest as the test runner. Mock DB calls with jest.mock() or a test-specific DB. Structure: test file per route, use beforeEach to reset state/mocks, afterAll to close DB connections. For integration tests, use a real test database (Postgres in Docker) that migrates and seeds before tests.', 'mid', 'testing', 'Backend Testing (Node.js)', '{"nodejs","supertest","jest","api-testing"}'::text[], true, true, now()),
('What is Testcontainers and when is it the right choice?', 'Testcontainers is a library that spins up real Docker containers (Postgres, Redis, Kafka) for integration tests. Instead of in-memory fakes (which don''t replicate real DB behavior) or shared test DBs (shared state = flaky tests), each test suite gets an isolated real container. Supports .NET, Java, Node.js, Go. Best for: DB-heavy integration tests where you need real SQL semantics (constraints, indexes, transactions). Slower than in-memory but catches real bugs mocks miss.', 'senior', 'testing', 'Backend Testing (Node.js)', '{"testcontainers","integration-tests","docker"}'::text[], false, true, now()),
('How do you test async code in .NET (async/await)?', 'xUnit supports async test methods natively — just make the test method `async Task`. `[Fact] public async Task GetUser_ReturnsUser() { var result = await service.GetUserAsync(1); Assert.NotNull(result); }`. For testing exceptions in async: use `await Assert.ThrowsAsync<NotFoundException>(() => service.GetAsync(-1))`. Don''t use `.Result` or `.Wait()` in tests — can cause deadlocks. Use CancellationToken where applicable.', 'mid', 'testing', 'Backend Testing (.NET)', '{"aspnet","async","xunit","testing"}'::text[], false, true, now()),
('What is pytest and what makes it powerful for Python backend testing?', 'pytest is Python''s most popular test framework. Key features: fixtures (reusable setup with dependency injection — `@pytest.fixture`), parametrize (`@pytest.mark.parametrize`), auto-discovery of test files. For FastAPI: use `TestClient` or `httpx.AsyncClient` with `anyio`. Fixtures for DB: create a test DB, apply migrations, yield client, teardown. Popular plugins: pytest-asyncio (async tests), pytest-cov (coverage), factory-boy (test data factories), pytest-mock (mock wrapper).', 'mid', 'testing', 'Backend Testing (Python)', '{"python","pytest","fastapi","testing"}'::text[], false, true, now()),
('How does authentication work in a Supabase application?', 'Supabase Auth provides secure user management using GoTrue under the hood. You can authenticate users via email/password, magic links, or OAuth providers. When a user logs in, they receive a JWT. The client library automatically attaches this JWT to subsequent database requests. PostgREST parses the JWT and sets Postgres configuration variables, allowing Row Level Security (RLS) policies to apply auth logic securely at the database layer.', 'junior', 'backend', 'Supabase', '{"supabase","auth","jwt"}'::text[], false, true, now()),
('What is Row Level Security (RLS) in Supabase and why is it important?', 'Row Level Security (RLS) is a PostgreSQL feature used heavily by Supabase to secure data at the database layer instead of the API layer. It allows defining policies (using SQL) that restrict which rows a user can SELECT, INSERT, UPDATE, or DELETE. In Supabase, the user''s JWT is decoded into Postgres connection variables (e.g., auth.uid()), which RLS policies use to enforce ownership and permissions directly, eliminating the need for a custom middle-tier security layer.', 'mid', 'backend', 'Supabase', '{"supabase","rls","security","postgresql"}'::text[], true, true, now()),
('What is the difference between Postgres functions (RPC) and Edge Functions in Supabase?', 'Postgres functions (RPCs) run inside the database using PL/pgSQL or plv8. They are ideal for data-intensive operations, aggregations, or anything that requires direct, fast access to tables. Edge Functions are serverless TypeScript functions running on Deno globally across a CDN. They are better suited for integrating with third-party APIs (like Stripe or OpenAI), listening to webhooks, or offloading CPU-intensive tasks that shouldn''t tie up database resources.', 'senior', 'backend', 'Supabase', '{"supabase","edge-functions","rpc","architecture"}'::text[], false, true, now());